using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameManaga : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        GameManager.storyState = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
